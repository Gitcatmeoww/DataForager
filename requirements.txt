# Dependencies are declared in pyproject.toml.
# Install the package in editable mode with evaluation + dev extras:
#
#   pip install -e ".[eval,dev]"
#
# This file installs the same set for convenience.
-e .[eval,dev]
